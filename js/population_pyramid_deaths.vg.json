{
    "$schema": "https://vega.github.io/schema/vega-lite/v5.json",
    "data": { "url": "https://raw.githubusercontent.com/ChaiKeRou/3179/main/data/crude_death_age_gender.csv"},
    "transform": [
      {"calculate": "datum.sex == 2 ? 'Female' : 'Male'", "as": "gender"},
      {"calculate": "datum.sex == 2 ? -datum.people : datum.people", "as": "deaths"}
    ],
    "width": 300,
    "height": 200,
    "mark": "bar",
    "encoding": {
      "y": {
        "field": "Age group",
        "axis": null, "sort": "descending"
      },
      "x": {
        "aggregate": "sum", "field": "deaths",
        "title": "population",
        "axis": {"format": "s"}
      },
      "color": {
        "field": "gender",
        "scale": {"range": ["#675193", "#ca8861"]},
        "legend": {"orient": "top", "title": null}
      }
    },
    "config": {
      "view": {"stroke": null},
      "axis": {"grid": false}
    }
  }